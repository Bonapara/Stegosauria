<% @artists.each do |artist| %>
  <h2> <%= artist.first_name %> </h2>
  <p> <%= artist.last_name %> </p>
  <p> <%= artist.biography %> </p>
  <p> <%= artist.domain %> </p>
  <p> <%= link_to "Voir l'artiste", artist_path(artist) %> </p>
<% end %>
