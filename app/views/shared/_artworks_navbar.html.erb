<div class="artworks-navbar">
    <!-- ONGLETS -->
    <!-- SCULPTURES -->
    <%= form_tag artworks_path, method: :get, class: "form-inline" do %>
    <%= submit_tag 'Sculpture', class: "navbar-button" %>
    <% end %>
    <!-- PAINTINGS -->
    <%= form_tag artworks_path, method: :get, class: "form-inline" do %>
    <%= submit_tag 'Peinture', class: "navbar-button" %>
    <% end %>
    <!-- PHOTOGRAPHS -->
    <%= form_tag artworks_path, method: :get, class: "form-inline" do %>
    <%= submit_tag 'Photographie', class: "navbar-button" %>
    <% end %>
    <!-- DRAWINGS -->
    <%= form_tag artworks_path, method: :get, class: "form-inline" do %>
    <%= submit_tag 'Dessin', class: "navbar-button" %>
    <% end %>
    <!-- CERAMICS -->
    <%= form_tag artworks_path, method: :get, class: "form-inline" do %>
    <%= submit_tag 'Céramique', class: "navbar-button" %>
    <% end %>
    <!-- INSTALLATIONS -->
    <%= form_tag artworks_path, method: :get, class: "form-inline" do %>
    <%= submit_tag 'Installation', class: "navbar-button" %>
    <% end %>
    <!-- INSTALLATIONS -->
    <%= form_tag artworks_path, method: :get, class: "form-inline" do %>
    <%= submit_tag 'Edition', class: "navbar-button" %>
    <% end %>
</div>

